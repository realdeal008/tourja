# Edit plan

## Information gathered
- `app/Component/About.tsx`: contains apostrophes in text (e.g., `Kingston's`, likely triggering `react/no-unescaped-entities` at lines ~34-35).
- `app/Component/BackgroundSection.tsx` and `app/Component/Footer.tsx`: use plain `<img>` elements, triggering `@next/next/no-img-element` warnings.
- `app/page.tsx`: appears to define `App` variable which eslint reports as assigned but never used (`@typescript-eslint/no-unused-vars`).
- Project uses an ESLint config based on `eslint-config-next`.

## Plan
1. Fix `react/no-unescaped-entities` in `app/Component/About.tsx` by escaping apostrophes (or using `&apos;` / `’` consistent with JSX best practices) for offending strings.
2. (Optional) Address `@next/next/no-img-element` warnings by switching `<img>` to `next/image` in:
   - `app/Component/BackgroundSection.tsx`
   - `app/Component/Footer.tsx`
   If doing this, ensure styling/refs still work (parallax transforms).
3. Fix `@typescript-eslint/no-unused-vars` in `app/page.tsx` by exporting the component properly (Next.js `app/page.tsx` should default export a page; remove unused `App` identifier if needed).
4. Run `npm run lint` to confirm no errors remain.

## Dependent files to edit
- `app/Component/About.tsx`
- `app/Component/BackgroundSection.tsx` (if converting to `next/image`)
- `app/Component/Footer.tsx` (if converting to `next/image`)
- `app/page.tsx`

## Followup steps
- Run `npm run lint`.
- If image conversion is done, run `npm run build` to ensure Next.js image requirements are met.

